#pragma once

#include <iostream>
#include <sstream>
#include <string>
#include <memory>
#include <vector>
#include <format>

namespace Engine::Helper
{
    std::vector<std::string> splitString(std::string input, char splitChar);
};